# LDAPBundle


```yaml
agixo_ldap:
    driver:
        host: localhost
        baseDn: 'dc=example,dc=com'
        bindDn: 'cn=users,dc=example,dc=com'
        loginAttribute: uid
        port: 389
```
